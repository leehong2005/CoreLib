package com.lee.sdk.test.app;

import com.lee.sdk.app.BaseApplication;

public class CoreLibApplication extends BaseApplication {
    @Override
    public void onCreate() {
        super.onCreate();

        // GoogleAnalyticsBL.getInstance(this);
    }
}
